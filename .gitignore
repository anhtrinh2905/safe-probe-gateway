# Secrets. The API key lives here and nowhere else in the repo.
.env

# Machine output. `data/` is regenerated by scripts/up.sh + the tool; `logs/`
# is regenerated by ui/streamlit_app.py, one file per browser session; deleting
# either must never lose thinking. See AGENTS.md.
data/
logs/

__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.venv/
.DS_Store
.cache_ggshield
