# Secrets. The API key lives here and nowhere else in the repo.
.env

# Machine output. `data/` is regenerated by scripts/up.sh + the tool; deleting it
# must never lose thinking. See AGENTS.md.
data/

__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.venv/
.DS_Store
.cache_ggshield
